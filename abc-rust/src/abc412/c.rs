use proconio::input;
// use std::collections::*;

fn main(){
    let stdin = io::stdin();
    let mut lines = stdin.lock().lines();

    let first_line = lines.next().unwrap().unwrap();
    let t = first_line as usize;

    for i in 0..t{
        lines
    }

}