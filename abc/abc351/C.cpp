#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define _GLIBCXX_DEBUG

int main(){
  int n;
  cin >> n;
  vector<int> ball(n);
  deque<int> row;

  rep(i, n){
    cin >> ball.at(i); 
  }

  rep(i, n){
    if(row.empty())
    
  }



}